// /** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains : ["localhost", 'img.dinamalar.com' ,'w.kalvimalar.com'],
  },
}

module.exports = nextConfig
